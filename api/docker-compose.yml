services:
  mongo:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  api:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mongo
    environment:
      MONGODB_URI: mongodb://mongo:27017/cortextrack
    env_file:
      - .env
    volumes:
      - ./src:/app/src
    command: npm run dev

volumes:
  mongo_data:
